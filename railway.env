# Production Environment Variables for Railway

# Database (Railway otomatik değişkenlerle değişecek)
DB_HOST=${{MYSQL_HOST}}
DB_USER=${{MYSQL_USER}}
DB_PASSWORD=${{MYSQL_PASSWORD}}
DB_NAME=hesabettin2

# JWT Secret
JWT_SECRET=hesabettin_super_secret_key_2024

# Server
PORT=${{PORT}}
NODE_ENV=production 